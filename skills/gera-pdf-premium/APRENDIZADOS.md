# Aprendizados da skill gera-pdf-premium

Memória viva da skill. **Leia antes de começar** (passo 0) e **atualize no fim** (passo 12).
Cada entrada diz o que aconteceu, a causa e onde a correção foi aplicada — para que o mesmo erro não se repita.
Falhas do motor são registradas sozinhas em `aprendizados/ocorrencias.jsonl` (`gerar ocorrencias` lista as não revisadas).

## Preferências do usuário

(nenhuma ainda — registre aqui identidade de séries, padrões de nome, créditos recorrentes e o que o usuário prefere cortar ou manter)

## Regras aprendidas (aplicadas nas instruções, nos conversores, no motor ou no CSS)

| Data | Sintoma | Causa | Correção aplicada |
|---|---|---|---|
